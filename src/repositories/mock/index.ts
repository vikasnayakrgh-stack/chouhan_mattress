// Phase B: Mock repositories deprecated — use '@/repositories' factory instead.
// This file remains only for backwards compatibility during transition.
// It will be removed once all page/component imports are updated.

export { getRepositories } from '@/repositories'
export type { Repositories } from '@/repositories'